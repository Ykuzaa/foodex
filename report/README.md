# Food Detection Report

After running the notebook, copy or keep the generated figures in `report/figures/`, then build:

```bash
pdflatex report.tex
bibtex report
pdflatex report.tex
pdflatex report.tex
```

Run these commands from `food-detection/report`.
